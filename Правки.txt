🎨 Фронтенд (Пользовательский интерфейс)
Шапка сайта (Header)
(Готово)Sticky Header: Реализовать фиксацию шапки при скролле на всех страницах сайта.

Контент:

(Готово)Удалить раздел «Категории».

(Готово)Удалить кнопку «Админ-панель» (доступ только по прямому URL).

(Готово)Оставить в навигации строго 4 раздела.

(Готово)Поиск: Перенести строку поиска в правый край шапки. Реализовать живой поиск (Live Search): при вводе текста выводить выпадающий список подходящих товаров по названию.

Динамика: Заголовки и ссылки в шапке должны подтягиваться из базы данных (настраивается в админке).

Главная страница
Секции (Разделы ГС): Реализовать динамическое добавление разделов (например, «Популярные товары»). Количество разделов не ограничено.

(Готово)Дизайн: Реализовать чередование фонов (зебра). Если один блок имеет белый фон, следующий (Раздел ГС) — серый, и так далее.

Страница категорий
(Готово)Стилизация: Установить белый цвет фона (аналогично основному разделу на Главной).

Карточка товара
(Готово)Структура данных: Должны отображаться: «Наименование», «Цена», «Описание», «Характеристики».

(Готово)Характеристики: Выводить строго в один столбец.

Галерея: Реализовать поддержку нескольких фото. Основное изображение — крупно, дополнительные — в ряд под ним в виде миниатюр.

Навигация: Добавить кнопку «Назад» (стрелка) в левом верхнем углу для возврата в предыдущую категорию.

Интерфейс: Удалить кнопку «В избранное».

Баннеры
Размеры: Уменьшить высоту всех баннеров на 100 px.

Адаптивность: Реализовать замену изображений в зависимости от устройства (Desktop/Mobile версии).

⚙️ Админ-панель
Новые разделы и функционал
(Готово)Дашборд: Создать рабочую область с отображением реальной статистики 

Сделать рабочей историю последних изменений.

Управление Header/Footer: Новые разделы для редактирования текстовых полей и ссылок в шапке и подвале сайта.

Шаблоны характеристик: Новый раздел для создания пресетов (наборов) характеристик, чтобы выбирать их при создании товара, а не вводить вручную.

Управление контентом
Загрузка медиа: * Важно: Исключить прямую загрузку в Supabase Storage. Реализовать интеграцию с облачным хранилищем (например, Cloudinary, AWS S3 или аналоги).

Внедрить этот механизм для разделов «Товары», «Категории» и «Баннеры».

Товары:

Добавить поле для редактирования описания (Rich Text).

Возможность загрузки нескольких изображений.

Выбор шаблона характеристик из выпадающего списка.

Bugfix: Исправить баг, при котором затираются характеристики при редактировании товара.

Баннеры: Добавить поля для загрузки отдельных изображений под десктопную и мобильную версии.

Безопасность и UI
Авторизация: Реализовать полноценную систему входа с валидацией сложного пароля.

Интерфейс: Убрать отображение футера сайта внутри админ-панели.

📊 База данных (PostgreSQL / Supabase)
Необходимо расширить текущую схему для поддержки нового функционала.

Предлагаемые изменения в схеме:
Таблица products: Добавить колонку description TEXT.

Таблица banners: Добавить колонку image_mobile_url TEXT.

Новая таблица spec_templates: Для хранения шаблонов характеристик.

Новая таблица site_settings: Для хранения текстов шапки и подвала.

SQL

-- Пример добавления описания в товары
ALTER TABLE products ADD COLUMN description TEXT;

-- Пример таблицы для шаблонов характеристик
CREATE TABLE spec_templates (
  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  template_json JSONB NOT NULL -- хранение набора полей
);

-- Пример таблицы для динамических настроек Header/Footer
CREATE TABLE site_settings (
  key VARCHAR(50) PRIMARY KEY,
  value JSONB NOT NULL
);